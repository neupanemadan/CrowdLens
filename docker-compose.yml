services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - backend_uploads:/app/uploads
      - backend_db:/app/data
    env_file:
      - ./backend/.env
    restart: unless-stopped

volumes:
  backend_uploads:
  backend_db:
